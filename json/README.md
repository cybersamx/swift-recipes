# JSON Examples in Swift

An example of serializing/deserializing JSON from/to Swift objects using:

* Codable
* JSONSerialization

## Setup

Run the following:

```bash
$ swift Codable.swift
$ swift JSONSerialization.swift
```
